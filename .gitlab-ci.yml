image: pikalab/ci:jdk12oracle-gradle6-git

variables:
  GRADLE_OPTS: "--no-daemon --console=plain"
  CI: "true"

before_script:
  - chmod +x gradlew

cache:
  paths:
    - /root/.gradle/
    - gradle/
    - build/
    - .gradle/

build:
  stage: build
  script: gradle assemble $GRADLE_OPTS

test:
  stage: test
  script: gradle check $GRADLE_OPTS

publish-bintray:
  stage: deploy
  script: gradle clean publishAllToBintray $GRADLE_OPTS
  environment:
    name: Bintray
    url: "https://bintray.com/pika-lab/tuprolog"
  when: manual
